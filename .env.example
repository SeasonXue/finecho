# Claude Agent SDK 凭证（二选一，任一生效即可）
#
# 1) 订阅账号（Pro/Max）长效 OAuth token
#    生成方式：`claude setup-token`（交互式登录后会打印 sk-ant-oat01-... 开头的 token）
#    走订阅配额，不按 token 计费，但有并发/QPM 限制。
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-xxxxxxxxxxxxxxxx

# 2) API Key（从 https://console.anthropic.com/ 生成）
#    按 token 计费，无订阅限流；脚本化批量任务更合适。
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxx

# 可选：默认使用的模型 id（不设则用 agent.ts 里的 DEFAULT_MODEL）
# ANTHROPIC_MODEL=claude-sonnet-4-6
